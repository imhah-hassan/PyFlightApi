meta {
  name: GetToken
  type: http
  seq: 1
}

post {
  url: {{url}}/login
  body: json
  auth: none
}

body:json {
  { "username" : "marc", "password":"onepoint"}
}

vars:post-response {
  token: res.body.token
}
